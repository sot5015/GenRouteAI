split the images and then pass them through the heightmapToData.py (later I will make the result to be save in a form) save thee form and it will be oyr data set.
every links there format, give me some time to make them, and you break them
